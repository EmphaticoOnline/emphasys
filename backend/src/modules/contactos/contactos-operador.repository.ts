import pool from '../../config/database';
export type OperadorInput={numero_licencia?:string;tipo_licencia?:string|null;vigencia_licencia?:string|null;activo?:boolean};
export async function getOperador(contacto:number,empresa:number){return(await pool.query('SELECT id,numero_licencia,tipo_licencia,vigencia_licencia,activo FROM transporte.operadores WHERE contacto_id=$1 AND empresa_id=$2',[contacto,empresa])).rows[0]??null;}
export async function saveOperador(contacto:number,empresa:number,d:OperadorInput){if(!d.numero_licencia?.trim())throw new Error('numero_licencia es obligatorio');const v=[empresa,contacto,d.numero_licencia.trim(),d.tipo_licencia??null,d.vigencia_licencia??null,d.activo!==false];return(await pool.query('INSERT INTO transporte.operadores(empresa_id,contacto_id,numero_licencia,tipo_licencia,vigencia_licencia,activo) VALUES($1,$2,$3,$4,$5,$6) ON CONFLICT(empresa_id,contacto_id) DO UPDATE SET numero_licencia=EXCLUDED.numero_licencia,tipo_licencia=EXCLUDED.tipo_licencia,vigencia_licencia=EXCLUDED.vigencia_licencia,activo=EXCLUDED.activo,updated_at=now() RETURNING id,numero_licencia,tipo_licencia,vigencia_licencia,activo',v)).rows[0];}
export async function deactivateOperador(contacto:number,empresa:number){return(await pool.query('UPDATE transporte.operadores SET activo=false,updated_at=now() WHERE contacto_id=$1 AND empresa_id=$2 RETURNING id,numero_licencia,tipo_licencia,vigencia_licencia,activo',[contacto,empresa])).rows[0]??null;}
