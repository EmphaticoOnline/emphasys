# Rutas comunes del backend compartido. Incluir este bloque dentro de cada
# server de erp.emphasys.cloud y compass.emphasys.cloud.
#
# location ^~ /api/ {
#     proxy_pass http://127.0.0.1:7001;
# }
# location ^~ /auth/ {
#     proxy_pass http://127.0.0.1:7001;
# }
# location ^~ /uploads/ {
#     proxy_pass http://127.0.0.1:7001;
# }
# location ^~ /public/ {
#     proxy_pass http://127.0.0.1:7001;
# }

# Dentro del server { } de erp.emphasys.cloud:
# root /var/www/emphasys-backend/current/frontend-erp-dist;
# location / {
#     try_files $uri $uri/ /index.html;
# }

# Dentro del server { } de compass.emphasys.cloud:
# root /var/www/emphasys-backend/current/frontend-compass-dist;
# location / {
#     try_files $uri $uri/ /index.html;
# }

# index.html debe revalidarse; los assets de Vite llevan hash.
# location = /index.html {
#     add_header Cache-Control "no-cache, must-revalidate" always;
# }
# location ^~ /assets/ {
#     add_header Cache-Control "public, max-age=31536000, immutable" always;
#     try_files $uri =404;
# }
